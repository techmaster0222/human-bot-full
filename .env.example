# =============================================================================
# AdsPower Bot Engine - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values.
# DO NOT commit .env to version control!
# =============================================================================

# AdsPower Configuration
# Get API key from AdsPower settings
ADSPOWER_API_URL=http://localhost:50325
ADSPOWER_API_KEY=your_adspower_api_key_here

# IPRoyal Proxy Configuration
# Get credentials from https://iproyal.com/
IPROYAL_USERNAME=your_iproyal_username
IPROYAL_PASSWORD=your_iproyal_password
IPROYAL_HOST=geo.iproyal.com
IPROYAL_PORT=12321

# =============================================================================
# API Security (Optional - Production Recommended)
# =============================================================================

# API Key for authentication (leave empty for dev mode - no auth)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEY=

# CORS allowed origins (comma-separated)
# In production, specify your dashboard URL
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Rate limiting (requests per minute)
RATE_LIMIT=100/minute

# =============================================================================
# Optional Settings
# =============================================================================

# Default proxy countries (comma-separated)
# PROXY_COUNTRIES=US,UK,DE,FR

# Logging level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO
